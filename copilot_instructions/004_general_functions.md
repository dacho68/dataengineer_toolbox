# Objective 1
create me get_logger function in ../dataenf_toolbox/utils.py.  it shall use the logger form logging import

# Objectve 2
Add validation to VTableModel class in the model.py. If the table_type = EXTERNAL then the FileType must be DELTA