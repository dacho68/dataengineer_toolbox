# Objective 1 :
Generate the unit tests for VTableModel.

## Outputs:
Save the unit tests to ../tests


# Objective 2 :
Generate the unit tests that deserialize a list of VTableModel in json

## Outputs:
Save the unit tests to ../tests/test_vtable_serialization.py